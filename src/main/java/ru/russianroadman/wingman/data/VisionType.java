package ru.russianroadman.wingman.data;

public enum VisionType {

    STANDARD, // can only see visible objects
    HIDDEN, // can see hidden objects
    DELETED, // can see hidden and deleted objects
    ALL // can see everything no matter what

}
